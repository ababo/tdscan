--[[

Globals:
 * `view_state` - current element view state (see model::ElementViewState).

]]

-- Update global `view_state` variable for a given time.
function update_view_state(time)
	print("time ", time);
end
